$('#property_no_delete').html('<%= escape_javascript(render :partial =>'properties/no_eliminar')%>');
$('#property_no_delete').dialog({
  resizable: false,
  height: 'auto',
  width: 'auto',
  title: "Eliminar",
  modal: true,
  buttons: {
                "Aceptar": function()
                {
                    $("#property_no_delete").dialog("close");
                }
            }
});