$('#honorary_edit').html('<%= escape_javascript(render :partial =>'honoraries/edit')%>');
$('#honorary_edit').dialog({
  resizable: false,
  height: 'auto',
  width: 'auto',
  title: "Editar",
  modal: true,
  buttons: {
                "Guardar": function()
                {
                    $("#form_edit_honorary form").submit();
                },
                "Cancelar" : function()
                {
                    $("#honorary_edit").dialog("close");
                }
            }
});

$('#honorary_edit').keypress(function(e) {
    if (e.keyCode == $.ui.keyCode.ENTER) {
          $("#form_edit_honorary form").submit();
    }
});