$('#canton_no_delete').html('<%= escape_javascript(render :partial =>'cantons/no_eliminar')%>');
$('#canton_no_delete').dialog({
  resizable: false,
  height: 'auto',
  width: 'auto',
  title: "Eliminar",
  modal: true,
  buttons: {
                "Aceptar": function()
                {
                    $("#canton_no_delete").dialog("close");
                }
            }
});
