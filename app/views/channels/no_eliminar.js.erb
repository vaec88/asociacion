$('#channel_no_delete').html('<%= escape_javascript(render :partial =>'channels/no_eliminar')%>');
$('#channel_no_delete').dialog({
  resizable: false,
  height: 'auto',
  width: 'auto',
  title: "Eliminar",
  modal: true,
  buttons: {
                "Aceptar": function()
                {
                    $("#channel_no_delete").dialog("close");
                }
            }
});