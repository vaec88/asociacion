$('#piece_no_delete').html('<%= escape_javascript(render :partial =>'pieces/no_eliminar')%>');
$('#piece_no_delete').dialog({
  resizable: false,
  height: 'auto',
  width: 'auto',
  title: "Eliminar",
  modal: true,
  buttons: {
                "Aceptar": function()
                {
                    $("#piece_no_delete").dialog("close");
                }
            }
});